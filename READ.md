# Git command workflow

    $git checkout master // wechselt auf den master branch

    $git pull / $git oull oriign master  // holt sich den nuesten stand vom     master branch

## Branchwechsel zeugs

    $git checkout <branchname> // wechselt auf den <branchnamen> branch

    $git checkout -b <branchname> // erstelltund wechselt auf einen neuen branch mit dem stand vom von dem vorherigen

## Commit zeugs

    $git status //zeigt alle geänderten Dateien an

    $git add . / $git add <filename> //Fügt alle <.> oder bestimmte <filename> geänderten Dateien hinzu

    $git commit -m "<message>" //Comittet alle zuvor hinzugefügten Dateien mit der nachricht <message>

    $git push origin <branchname> //pusht die comitteten dateien auf den <branchname> branch in gitHub
